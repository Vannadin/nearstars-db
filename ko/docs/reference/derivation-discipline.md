<!-- 도출값을 사람 기억이 아니라 기계가 지키게 만드는 규율 — 왜 이렇게 정했는지의 근거 기록 -->
# 도출 규율. 레시피를 왜 이런 모양으로 만들었나

NearStars는 숫자를 많이 도출합니다. 천체의 쌍극자 자기장, 조석가열, 자기권계면 거리,
고리의 색 — 전부 방법론 문서가 측정값과 발표된 물리에서 어떻게 얻는지를 서술합니다.

그 방식은 작동했고, 동시에 계속 틀렸습니다. 크게 틀린 게 아니라 조용히요. 한 숫자가 세
군데 적혀 있다가 한 곳만 어긋난 채 몇 달을 가는 식으로요. 이 문서는 그래서 도입한 규율과,
각 규율이 답하는 실제 사고를 기록합니다. 나중에 읽는 사람이 어느 규칙이 하중을 받고
어느 것이 취향인지 구분할 수 있게요.

아래 인용한 사고는 전부 이 프로젝트에서 실제로 난 것입니다.

---

## 무엇이 계속 틀렸나

**값이 움직였는데 그 값에 매달린 것들이 안 따라갔습니다.** Proxima의 자기권계면 코 거리를
23.5에서 35.33으로 갱신했습니다. `outer_compression`과 `outer_extension`은 그 값의
함수인데 옛날 값 그대로 남았습니다. 아무도 못 잡았습니다. 세 숫자 사이의 관계가 그 행을
마지막으로 만진 사람 머릿속에만 있었으니까요.

**worked example이 그것이 예시하는 공식에서 떨어져 나갔습니다.** 다이나모 스케일링 표가
ε Ind A b의 쌍극자 모멘트를 지구의 약 3 700배로 적어놨습니다. 같은 문서의 거듭제곱
법칙으로 다시 계산하면 약 201 000배입니다. 54배 차이고요. 손으로 친 표였고, 입력이 바뀐
뒤로 다시 도출된 적이 없었습니다.

**두 문서가 공개적으로 서로 모순됐습니다.** 조석가열 방법론과 위성 에너지수지 방법론이
고체 조석항이 전역인지 국소인지를 두고 어긋나 있었습니다. 둘 다 발행됐고, 둘 다 쓰이고
있었고, 그 불일치에는 주인이 없었습니다. 아무것도 둘을 대조하지 않았으니까요.

**산문이 레시피의 적용 여부 자체를 가렸습니다.** 방법론 문서 중 유효범위를 밝힌 것이
몇 개인지 세어보니 "여덟 개가 없다"고 나왔습니다. 실제로 열어보니 진짜 없는 건 두 개였고,
나머지 여섯은 검색할 생각을 못 한 제목 아래 멀쩡히 적어놓고 있었습니다. 내용은
멀쩡했는데 형식이 그걸 못 찾게 만들었습니다. 우리 자신도 포함해서요.

**아무것도 거르지 않는 어휘.** Phase 4 게이트는 필드 이름을 메뉴에 대조하는 것처럼
보였습니다. 아니었습니다. 축 키의 접미사만 검사했고, 실제로 출하되는 필드 이름은 아무
검사도 안 받았습니다. 보드에 `ZZZ_totally_bogus_name`을 넣어도 오류 0으로 통과했습니다.
그 게이트 아래서 고유 필드 이름이 158개까지 늘었고, 그중 36개는 딱 한 번만 쓰였고,
`base_color`와 `base_colour`가 나란히 출하됐습니다. 이 중복은 감사 파일 세 곳에 적혀
있었습니다. 네 번째 파일은 그냥 두 철자를 다 메뉴에 등록해서, 드리프트를 합법으로
만들었습니다.

패턴은 매번 같습니다. **지식은 실재했지만 사람 안에 살았고, 사람은 잊습니다.**
이 중 물리를 틀린 건 하나도 없습니다.

---

## 규율

### 1. 레시피는 숫자가 아니라 상태를 돌려준다

모든 도출은 값과 **함께** 자기가 쓴 정확한 입력, 스스로 판정한 레짐, 왜 그렇게 됐는지를
기계가 쓴 한 문장, 신뢰등급, 인용, 수렴 여부를 돌려줍니다. 맨 숫자는 나중에 감사할 수
없지만 상태는 됩니다.

`engine/payload.py`에 있습니다.

### 2. 범위 밖은 예외가 아니라 반환값이다

다룰 수 없는 천체를 받으면 레시피는 그 사실을 이유와 함께 돌려줍니다. 예외를 던지지도,
더 나쁘게는 외삽하지도 않습니다. 거대행성 다이나모 레시피는 갈색왜성을 명시적으로
거절합니다. 그 가지가 필요로 하는 광도 트랙을 출처가 주기를 거부하기 때문입니다.
거절은 답입니다. 추측은 아니고요.

열수송 모순이 치른 대가가 정확히 이겁니다. 두 문서 다 거절하지 않아서 둘 다 적용됐습니다.

### 3. 앵커는 발표된 값으로, 우리 출력으로는 절대

레시피의 테스트는 다른 사람이 알려진 천체에 대해 발표한 숫자를 재현합니다. 목성 9.0 G,
ε Eri b 19 G를 명시된 허용오차 안에서요. 우리 출력을 우리 출력으로 시험하면 아무것도
검증되지 않습니다. 54배 표 오류를 드러낸 게 이 규율입니다.

### 4. 표는 생성하지, 손으로 치지 않는다

방법론 문서의 worked example은 그 문서를 구현한 코드가 다시 생성하고, 어긋나면 검사가
실패합니다. 손으로 친 표는 값의 사본이고, 사본은 어긋납니다.

`engine/dynamo_table.py`에 있고 `scripts/check.sh`에 걸려 있습니다.

### 5. 의존은 기억이 아니라 파일에 선언한다

`engine/chain.yaml`이 어느 도출이 어느 도출을 필요로 하는지 네 종류로 기록합니다.
`requires`(수치 입력), `selects`(어느 모델을 쓸지 고름), `influences`(값이 꼭 바뀌지는
않아도 재검토를 강제), `excludes`(검토하고 기각 — 처음부터 다시 고민하지 않도록 남김).
순환은 허용하되 근거와 함께 선언해야 하고, 선언 안 된 순환은 검사가 실패시킵니다.

그러면 `engine/chain.py affects <노드>`가 "뭘 다시 봐야 하나"에 답합니다. 예전에는
기억으로 답하던 질문입니다.

### 6. 출하된 값은 역류한다. 그것도 그려야 한다

방법론끼리의 의존 그래프만으로는 부족합니다. 여기는 큐레이션 저장소라 값이 결정되고,
적히고, 그다음 **다시 쓰이기** 때문입니다. `geopotential_j2 = 0.023`은 결론이 아니라
21시간짜리 위성 적분의 인자이고, 그 결과가 또 하나의 확정된 결정이 됩니다. 안정성
리포트는 이 편평도가 어느 위성 궤도를 고를지를 **뒤집는다**고 적고 있습니다. 내부 구조를
바꾸면 그 런이 다시 열립니다.

그래서 이 층은 세 관계를 더 기록합니다. 출하된 각 필드를 어느 노드가 낳아야 하는지,
어느 출하값이 다른 출하값에서 계산되는지, 무엇이 그것을 먹는지. Proxima 사고가 정확히
두 번째 종류이고, 방법론만 그린 그래프는 그걸 볼 수 없습니다.

`engine/bindings.yaml`과 `engine/backflow.py`에 있습니다.

### 7. 어휘는 밖의 것을 거절할 때만 실재한다

필드 이름은 단 하나의 명부에 대조되고, 명부 밖 이름은 실패합니다. 명부가 하나뿐인 이유는
둘이면 서로 어긋나기 때문입니다. 명부를 넓히는 행위 자체도 검사 대상입니다. 철자 변종은
메뉴에 등재될 수 없습니다. 지난번 중복이 감사 네 번을 살아남은 방법이 바로 그거였으니까요.

### 8. 산문은 설명하고, 타입 있는 필드가 값을 진다

결정을 산문으로 적어놓은 자리에서는 여러 양이 한 문자열에 뭉쳤습니다. 한 필드가 천체의
조성·질량·평형온도·구름등급을 한꺼번에 담고 있으면 어떤 기계도 그걸 해체하지 못합니다.
값은 타입 있는 필드에, 한 필드에 한 양과 그 단위로 들어가야 합니다. 산문은 그 값들을
설명하는 설정집 텍스트로 남습니다.

이미 승인된 산문은 다시 쓰지 않습니다. 할 일은 거기서 값을 꺼내는 것뿐입니다.

### 9. 물리 상태를 도출하고, 인코딩은 어댑터에 맡긴다

레시피가 만드는 것은 천체의 물리이지, 그걸 그려주는 무언가의 설정 파일이 아닙니다.
물리 상태를 타깃의 필드로 옮기는 일은 어댑터가 합니다.

```
레시피  →  물리 상태 (타깃 무관)  →  어댑터  →  KSP cfg
```

정돈 차원의 이야기가 아닙니다. 보드에서 `pause_*` 파라미터 열여섯 개를 손으로 관리하고
있었는데, 그건 물리가 아닙니다. 게임 엔진의 형상 함수 파라미터죠. 물리는 자기권계면
거리 하나뿐입니다. 한 값의 인코딩 열여섯 개를 손으로 들고 있던 것이 Proxima 사고를
만들었고, 방사선대 셸 필드에서 같은 모양이 반복됩니다. 물리 상태로 기록하면 인코딩은
매 빌드마다 다시 생성되고, 출처와 어긋날 수가 없습니다.

그리고 이건 바닥이 없어 보이던 질문에도 답합니다. 어휘가 영원히 늘어나는 것을 무엇이
막는가. 두 층이 각자의 이유로 묶여 있습니다. 엔진은 물리가 묶습니다 — 천체의 상태는
유한한 물리량 목록이고, 들어올 자격은 *"단위를 가진 양인가, 아니면 선언된 무차원·범주
상태인가"* 로 정해집니다. 서술은 양이 아닙니다. 어댑터는 우리 것이 아니고 우리 야심에
따라 늘지도 않는 스키마가 묶습니다. 두 경계는 서로를 밀지 않습니다.

### 10. 좌석이 주장에 빚지는 것

위의 아홉 규율은 기계가 값에 빚지는 것입니다. 이 절은 사람이 주장을 옮기는 동안 — 브리프에서,
보고에서, 노트에서 — 그 주장에 빚지는 것입니다. 모든 줄은 한 번 터진 뒤에 쓰였고, 이 절을
엮은 날의 오류 열넷 중 게이트가 잡은 것은 0이었습니다. 빠져나간 것은 틀린 값이 아니라
**얻지 않은 지위를 달고 있던 맞는 값**이었습니다. 규칙은 한 줄에 하나이고, 규칙 아래 들여쓴
줄은 그 규칙에 합쳐진 사례로, 증거로 남긴 것입니다. 괄호는 `engine/SESSION-HANDOFF.md`
`2269a8d5` 에서 그 사례가 살던 줄이고, 그런 줄이 없는 사례는 브리프 번호입니다.

**없다 · 몇 개다 · 누구 탓이다 — 이 셋은 조회를 붙이지 않으면 말하지 않는다.**

**옮겨지는 숫자가 지니는 것**

- **숫자는 라벨 없이 들어오지 못합니다.** 양의 이름, 위치, 조건을 원문 그 자리에서 확인합니다. "5500–6300 K" 가 아니라 "중심 온도 5500–6300 K, Scheibe 표 1" 입니다. 세 좌석에서 한 번씩 터진 같은 병입니다.
  - ↳ *거짓 출처. 거짓 라벨은 라벨 없음보다 나쁩니다. 라벨이 확인을 멈추게 하니까요. 보내는 쪽은 인용부호 안의 것만 인용하고, 받는 쪽은 자기에게 돌려진 숫자를 실제로 보낸 것과 대조합니다.* (276줄)
  - ↳ *브리프의 숫자는 조건과 커밋 앵커를 함께 지닙니다. 하루 네 오류 중 셋이 맞는 숫자를 잘못 짝지은 것이었고, 넷째는 부등식을 끝까지 밀지 않은 것이었습니다.* (280줄)
  - ↳ *±도 숫자의 일부이고 조성도 그렇습니다. 자릿수는 남기고 둘 중 하나를 떨어뜨린 전달은 숫자를 전달한 것이 아니며, 요약에서 가장 심하게 샙니다.* (304줄)
  - ↳ *선언된 창 안에서는 어느 가장자리에서 잰 값인지 말합니다. 코드가 실제로 계산하는 곡선을 기준으로 삼습니다.* (315줄)
  - ↳ *출처가 밝혀지지 않은 숫자는 출처 없이 인용합니다. 지어낸 출처는 빠진 출처보다 잡기 어렵습니다.* (411줄)
  - ↳ *0.717 Myr 이 출처 없이 시간상수 행세로 브리프 둘에 실려 들어갔습니다. 그 값은 ²⁶Al 반감기이고, 자릿수가 우연히 겹쳤을 뿐입니다.* (브리프 156–157)
- **인쇄된 값만 여행합니다.** 한 천체의 숫자를 다른 천체로 옮기기 전에, 그것이 원문에 인쇄된 값인지 첫 천체의 상태로 도출된 값인지 확인합니다. 지구의 3040 K 는 4800 K 를 지구의 단열비로 나눈 것이고, 화성에는 화성의 비가 있습니다.
- **밴드가 노브의 반대말인 것은 그 폭이 물리에서 올 때만입니다.** 우리 코드가 우연히 실패하지 않은 구간을 폭으로 삼으면 그것은 수치 경계이고, 그걸 실어 보내면 실패를 범위로 출하하는 셈입니다.
- **판정을 바꾸는 숫자는 보고되기 전에 재현됩니다.** 아래로는 브리프에 라벨 없는 숫자가 없고, 위로는 행을 열거나 닫는 숫자, 일치를 주장하는 숫자, 앵커나 등급을 움직이는 숫자를 지휘석이 다시 도출합니다. 감사의 다리는 따로 둡니다. 다리 셋이지, 옮겨 놓은 하나가 아닙니다.
- **숫자 먼저, 해석은 다음에, 메시지를 나눠서 보냅니다.** 그래야 두 좌석이 숫자를 독립적으로 읽습니다. 섞어 보내면 읽기가 검증이 되지 않습니다.

**부정은 도구를 지닙니다**

- **좁은 도구의 출력은 일반 사실이 아닙니다.** 발견 옆에 도구를 적고, 발견이 부정이면 그 도구가 보지 못하는 것을 적습니다. 제목 스캔은 점성 출처를 하나도 못 찾았고 전문 검색은 다섯을 찾았습니다.
  - ↳ *"못 찾았다"는 정당한 결말입니다. 무엇을 어떻게 찾았는지 적습니다.* (345줄)
  - ↳ *맞는 질의의 잘린 화면은 맞아 보이는 틀린 답입니다. `head`, `tail`, `cut` 은 소리 없이 자르니, 부정이나 개수를 보고하기 전에 필터 없이 다시 돌리거나 잘림을 보이게 합니다.* (403줄)

**사전 등록**

- **돌리기 전에 결과를 등록하고, 돌린 뒤에 등록되지 않은 결과의 종류를 등록합니다.** 그래야 분류가 배웁니다. 재시도에는 다섯 갈래가 표준이고, "원문이 판정 구간에 닿지 않는다"와 "원문이 두 후보와 다 어긋난다"도 그 안에 있습니다.
  - ↳ *판별 시험은 예측마다 그 가정을 옆에 적고, "모든 예측이 빗나감"을 시험 자체의 실패로 등록합니다. 그때의 산출물은 틀린 가정의 이름입니다.* (333줄)
- **답이 맞게 나올 때까지 선언을 조율하지 않습니다.** 선언하고, 적분하고, 보고합니다. 연속 세 항목이 간극을 닫아 줄 값을 쓰기를 거절했고, 한 번은 0.0012 차이였습니다.
- **자기 오차가 기준보다 큰 검사는 등급을 올릴 수 없습니다.** 그 거울상도 있습니다. 검사는 정밀한데 검사받는 것이 그냥 틀릴 수 있습니다.
- **검사는 결과가 기록되기 전에 통과할 수도 실패할 수도 있음을 증명해야 합니다.** 늘 발화하는 검사는 상수입니다. 시험에도, 숫자가 보고되는 스크래치 스크립트에도, 셸 한 줄에도 같이 적용됩니다.
  - ↳ *읽기엔 맞았는데 돌리면 틀린 검사 셋 — `pgrep -x python3`, `pgrep -P`, `git diff --quiet` — 중 첫째가 건강한 게이트 둘을 죽였습니다. 실패할 경우만이 아니라 통과할 경우에도 검사를 겨눕니다.* (1447줄)
- **브리프가 이름 붙인 것을 짓기 전에 읽습니다.** 기존 코드, 선언된 한계, 논문의 문장, 우리 자신의 규칙. 하루 일곱 번 중 일곱 번, 읽기가 지을 것을 바꿨습니다.

**운영 규칙 둘**, 어느 군에도 안 들지만 다른 곳에 적힌 데가 없어 남깁니다

- **`engine/` 에 새 런타임 의존을 넣지 않습니다.** `check.sh` 는 시스템 파이썬으로 돕니다.
- **작업 세션의 컨텍스트는 항목 경계에서 비웁니다.** 다음 세션에 필요한 것, 특히 시도하고 버린 접근이 노트에 실렸는지 확인한 뒤에요. `/clear` 는 오너가 칩니다. 세션은 스스로를 비울 수 없습니다.

**함께 보기** — 이미 다른 곳에 사는 규칙은 포인터 한 줄씩, 그 아래 합쳐진 사례

- 앵커는 비트 단위로 동일하게, 움직인 숫자는 원인과 함께 보고, `--refresh` 는 같은 커밋에 → `engine/test_ice_giant.py@«--refresh»` 가 강제합니다. 원칙은 위 §3.
- 게이트 FAIL 0 → `CLAUDE.md@«8. Run Tests Before Marking Complete»`. 넓힌 부분은 내 작업이 게이트 시간에 얼마를 더하는지 말하는 것.
  - ↳ *실행 시간 추정은 그것을 잰 커밋에 속합니다.* (373줄)
  - ↳ *`check.sh` 는 약 20분이고 전경 툴 한도는 10분입니다. 백그라운드로 돌리고 로그 꼬리를 봅니다. 외부 신호의 발신자 후보에는 하네스 자체도 들어갑니다.* (430줄)
- 논문은 `ADS_API_TOKEN` 과 캐시로, WebSearch 금지, 식별자는 읽어서 제목으로 확인 → `CONVENTIONS.md@«3.2 ADS paper discipline»`, `CONVENTIONS.md@«3.3 Paper references are links»`.
  - ↳ *캐시 파일은 PDF 임이 보인 뒤에만 보유한 것입니다. `file` 을 돌립니다. `.PROVENANCE.txt` 는 어떻게 얻었는지의 기록이지, 그 부재가 검사는 아닙니다.* (349줄)
  - ↳ *오너에게 논문을 청하기 전에 ADS `esources` 를 봅니다. 일곱 요청 중 셋은 이미 질의한 필드에 무료 경로가 있었습니다.* (398줄)
- 커밋은 영어로, 논리 단위 하나, 고정 identity → `CONVENTIONS.md@«1.8 Git commit language»`, `CONVENTIONS.md@«1.9 Git identity»`, `CLAUDE.md@«9. Semantic Commits»`.
- 구멍을 닫는 커밋은 그 구멍을 재현하는 시험을 가져옵니다 → `CLAUDE.md@«4. Goal-Driven Execution»`.
- 프로세스 목록은 상태가 아니라 순간입니다. 다시 띄우려는 세션은 스스로 확인합니다 → `engine/tools/README.md@«Telling a busy gate from a dead one»`, `engine/tools/README.md@«A gate is a process group»`.
- 다른 것이 다시 켜는 변수에 실린 센티널은 계약이 아닙니다 → `engine/interior-core.md@«A sentinel carried in a variable something else re-arms is not a contract»`.
- 차가운 측면의 둘째 형태. 조향은 결론도 낼 수 있어야 합니다 → `engine/interior-core.md@«the climb hid the wall from the controller»`.
- 제3자는 솔버를 A/B 가 아니라 폐합으로 검사합니다 → `engine/interior-core.md@«A third party checks a solver by closure»`.
- 닿을 수 없는 경계 조건은 이름 붙은 거절로 닫습니다 → 위 §2.
  - ↳ *출처를 굽는 것은 그 주장 범위를 사전 등록한 물리성 기준으로 훑은 뒤에, 그리고 실효 상한을 표와 함께 기록한 뒤에. 인쇄된 유효 범위는 실행된 범위가 아닙니다.* (328줄)
- 새 숫자는 옛 숫자들을 지우는 데 쓰이기 전에 그들에게로 되돌려 대조합니다 → 위 §5, §6 (`chain.py affects`, 역류).
  - ↳ *숫자를 실은 산문은 그것을 갱신할 의무를 집니다. 조건을 실은 산문도, 예측을 실은 행도 그렇습니다.* (326줄)
- 표와 추출 → 위 §4.
  - ↳ *재생성한 표가 다르면 별도 추적으로 가립니다. 이름만으로 표를 코드에, 코드를 표에 맞추지 않습니다.* (331줄)
  - ↳ *숫자 표를 기계로 추출할 때는 다른 렌더링과 대조합니다. 같은 추출에 실려 온 검사값과는 대조가 아닙니다.* (427줄)
- 파일이 든 것이 아니라 내가 쓴 것을 스테이징합니다. 공유 worktree 에서는 `git add` 전에 `git diff --stat` → `engine/tools/README.md@«reading `git diff --stat`»` (이 규칙의 다른 집은 repo 밖 오너의 세션 메모리입니다).
- 탐지기를 넓히지 않고, 탐지기가 이미 읽는 모양으로 데이터를 씁니다 → 위 §7, `engine/tools/README.md@«Four times the answer was to reshape the input»`.
- 개수는 그 밑의 문장만큼만 참입니다 → `engine/tools/README.md@«Counts and quotations are both only as true as the sentence over them»`.
- 앵커를 다시 겨누면 그 주위 문장을 다시 읽습니다 → `engine/tools/README.md@«Re-aiming an anchor is not the whole repair»`.
- 이름이 증거였고, 이름이 틀렸습니다 → `engine/tools/README.md@«Four names that read correctly and were wrong when checked»`.

---

## 이걸로 해결되지 않는 것

이 규칙들은 오류를 **감지 가능하게** 만듭니다. 도출을 옳게 만들어주지는 않고, 실제 비용도
있습니다. 새 값 하나마다 바인딩과 인용, 대개는 테스트까지 필요합니다.

두 구조적 공백 가운데 하나는 2026-08-25에 닫혔습니다. 내부 구조는 이제 가정이 아니라
도출입니다. 레시피 네 개가 각자 사적으로 가정하던 층 밀도가 적분의 출력이 되었고, 그
자리를 대신하던 조회표 둘도 함께 사라졌습니다. 솔버가 못 푸는 것은 이제 **기작의 이름을
대며 거절**하는데, 남은 공백이 실제로 손댈 수 있는 일이 되는 지점이 바로 여기입니다.
알파 센타우리 위성 셋은 "밀도가 낮다"가 아니라 "얼음 III·V·VI의 상태방정식이 필요하다"로
돌아옵니다.

나머지 하나는 여전히 열려 있습니다. 출하된 필드 열아홉 개는 그것을 낳는 노드가 아예
없습니다. 대부분 항성의 외관과 활동이고, 둘(`color`, `ring_color`)은 방법론 문서는 있는데
그래프에 자리가 없습니다.

## 관련

- `engine/chain.yaml`, `engine/bindings.yaml` — 두 선언
- [방법론 인덱스](methodology-index.md) — 레시피 자체
- [툴](tools.md) — 실행할 것
