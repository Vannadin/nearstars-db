# C13 — does the fuzzy core account for the moment-of-inertia deficit? — checklist

Brief 21, opened 2026-08-31 on the C/MR² measurement (−15.8 % / −11.4 % after the radius is
stripped). Not C5's repair — C5(a) was true when written; what is new is a measurement.
Order fixed by the brief: bound the non-core terms → bracket with what exists → only then
build (if the bracket reaches).

- [x] Read first: C5 row (both halves) · `giant-residual-context-notes.md` · the C/MR²
      comparison and its notes
- [x] Bound the rotation term → verify: derived with labels — axial-vs-mean moment
      (2/3)J₂ = 1.02 % / 0.98 % of λ; the rotational parameter m_rot = ω²R_eq³/GM =
      2.95 % / 2.61 % as the O(m) scale of any figure-driven restructuring
- [x] State the rotation-period spread as a target-side spread → verify: −3.3 % / +6.0 %
      (already in the gate); attribution measured against the P_Voy target, reason written
- [x] State what λ is (a gravity-field-derived quantity, not a measurement) in the row
- [x] Bracket, end B attempted: same rock mass as uniform envelope Z (`envelope_z`, no
      silicate layer) → **the recipe cannot hold it**: full solves refuse in 1 s, and a
      standalone integration at the anchors' own convergence points refuses at stack build
      (T = 0.0) — with no silicate at the centre the water column reaches P_c
      (1220 / 1533 GPa) and the solid ladder's evidence span ends at 1000 GPa
      (French & Redmer 2015 knots via SeaFreeze); the cold-phase pre-check demands that
      coverage. Branch 2's assumption ("nothing in the recipe caps the outer extreme")
      is false, with the cap named
- [x] Ceiling computed anyway, outside the engine, assumptions labeled → all rock moved to
      the surface: Δλ/λ ≤ f_rock/λ = +31.2 % / +33.7 %, above the required
      +18.7 % / +12.9 % — so the idea **cannot be excluded** by the bracket; it also is
      not confirmed
- [x] C13 row written: opened 2026-08-31, left **open** — the bracket did not settle it
      either way; what settling requires is named (the cold-flank representability work,
      same family as the Queyroux–Neptune route death)
- [x] C5 revisited line: (a)'s "no consumer" superseded — a consumer (the measured deficit)
      now exists; the blocker moved from consumers to representability
- [x] Helled & Stevenson 2017 applicability — never reached (only relevant if building);
      recorded as the untouched fourth branch
- [x] No code, no anchor, no gate change (docs only; the last full check.sh of this session
      stands)
- [x] Report to nearstars-cb; judgment-changing numbers flagged for directing-session
      reproduction (both refusals reproduce in ≤ 1 s)
