<!-- C16 — 암석 다이나모의 영역 게이트(국소 로스비 수). 분기 구조 + 자유 자전 경로는 짓고, 잠김 경로의 Ro_ℓ 은 이름 대고 거절. 사전등록 → 실행 기록 -->
# Regime gate — local Rossby number (C16) — context notes

2026-09-04. **§1–§3 are the pre-registration, committed before any code ran.** Directing seat's rewrite after the
parallel seat's second survey; every sentence below that quotes RM22 was reproduced by the work seat from the
cached `2203.01065.md`.

## 1. What changed, three times

    handoff       "do not build, close it"           ← wrong: said no formula exists; one does
    survey 1      "buildable"                        ← optimistic: the formula exists but does not close
    survey 2      "printed, but not reproducible"    ← now

**The printed Ro_ℓ chain** (RM22 §5.2, eqs for F, Ra_Q, Ro, Ro_ℓ; Table 6 inputs: λ_t 1×10⁻⁵ m²/s, λ_m 1.32 m²/s,
α 1.3×10⁻⁵ /K, C_p 850 J/kg/K; threshold Ro_ℓ = 0.12, OC06, base-heated):

    F    = α g q_conv / (ρ C_p)          Ra_Q = r* F / (D² Ω³)          Ro = u/(Ω D)
    Ro_ℓ = (0.58 / ν^(1/3)) (λ_m/λ_t)^(1/5) Ra_Q^(2/5) Ω^(1/3) D^(2/3)

**Three holes, each named, each fatal on its own for the locked branch:**
1. **ν has no value anywhere in RM22.** Appendix A.2's η_c is *"the dynamical viscosity of a mineral"* in eq. 36's
   Ra_c — a different equation and a different layer, explicitly a different substance. The exponent is weak
   (ν^(−1/3); the authors: *"depends mainly on the Rayleigh number, Ω, and D, and weakly on the core
   thermodynamic properties"*) but it is one unknown input, declared as such.
2. **q_conv is not defined.** One phrase — *"the convective heat flux generated by the core"* — and no equation;
   the appendix ends at Q_c (W) and the text starts at q_conv with no sentence joining them. Dimensionally
   W/m² (parallel seat's derivation, not printed). Total or super-adiabatic excess is undecided, and both
   readings have textual support. **Our Earth's 2.75 vs 4.91 TW (Brief 60 / C14) hangs exactly here.**
3. **The printed equation does not reproduce the paper's own Table 8** (parallel seat's scaling test on the five
   Solar-System bodies, normalised so ν and λ cancel): Ro_ℓ ∝ Ra_Q^(2/5) alone gives Mercury 0.94 · Venus 1.24 ·
   Mars 0.88 · Ganymede 0.75 of the printed values; the full printed form (with Ω^(1/3) D^(2/3)) gives 0.26 ·
   0.20 · 0.96 · 1.02 — **4–5× off on the two slow rotators**, and the least-squares exponent (P^1.156) matches
   Ra_Q^(2/5) alone (P^1.200), not the full form (P^0.867). The cause is the Ω factor, not D. Whichever form is
   chosen disagrees with the paper somewhere. **And Table 8 is not a validation**: *"we have found that the value
   for the core thermal conductivity better fitting the observed magnetic moment and local Rossby number is
   60 W/m/K"* — k was fitted to that table.

**Three of RM22's own defects go to the index** (`paper-defects.md`): the Ro_ℓ equation vs its Table 8 (4–5×);
λ_m = 1.32 m²/s against its own eq. 24, 1/(μ₀σ) with σ = 1.36×10⁶ S/m → **0.585**, a 2.25× disagreement with
both values sourced to (D); 0.12 called a *"local Rossby number"* in one place and a *"Rayleigh number"* in
another. (Row 13 already carries the comma-decimal Mars mass and the three collapse numbers.)

## 2. What is built, and what is refused by name

**RM22's branch structure is explicit and printed** (§5.2): *"If the planet is tidally coupled, we can estimate
the most probable spin-orbit resonance following Dobrovolskis (2007), and then, its angular velocity. If the
planet is not tidally coupled, we assume free rotation leading to a dipolar magnetic moment, like of all the
objects of the solar system with this characteristic. In this case we use equation 20 for estimating ℳ."* And
in the dipolar zone *"it does not present an explicit dependence on the angular velocity of the planet."*

- **Built**: the branch. A free rotator does not pass through Ro_ℓ at all — it is **dipolar by the paper's
  rule** and takes the ladder's base moment (eq. 20 is what the ladder already anchors). A locked body enters
  the locked branch, whose Ro_ℓ is **refused by the three names above** — a far more informative state than
  today's `status: gap`.
- **Corrected**: the edge note *"a Rossby number cannot be derived from a rotation period"* is wrong — Ω is
  exactly what `rotation_period` supplies; what blocks Ro_ℓ is ν, q_conv and the equation/Table 8 mismatch.
- **Not built**: any Ro_ℓ number. No ν declared, no q_conv definition elected, no form chosen against the paper.

**A fact found while reading the wiring, before code**: the supplier node `tidal_locking` (`chain.yaml`, outputs
`[locked, t_lock, rotation_period]`) **has no recipe** — nothing registers it, and the Earth/Pandora chains
print no `tidal_locking` line. So the branch's own input, `locked`, is today a **declaration** (a body's
`dynamo_regime`, or a `locked` flag) and not a computed value; the branch is wired to consume the node's
outputs the day it exists, and takes a declaration until then, saying so. Roster: Pandora is locked to
Polyphemus at 32 h **on the Phase 4 board**, not in `bodies/pandora.yaml` — Earth and Pandora both reach the
gate only through a declaration today.

**Measurement (C5 question), instrument first**: since Ro_ℓ cannot be computed, the multipolar branch can only
be entered by a *declared* `dynamo_regime: multipolar`. Count, over the roster chain runs, how many results
actually reach `MULTIPOLAR_FACTORS` (a spy on the ladder's multipolar branch; positive control = a synthetic
declared-multipolar call). **If zero, that is reported as a C5 question for the owner — not judged here.**

**Pozzo's withdrawal, re-stated as a conditional**: RM22 prints both λ_t = κ/(ρC_p) (eq. 23) and λ_m = 1/(μ₀σ)
(eq. 24) and *"estimated their variation with pressure using Pozzo et al. (2012)"*; Table 6's σ and λ_m are (D)
= Pozzo 2012. Our own recipe still computes no σ, so the withdrawal's ground stands **as long as we do not run
RM22's chain beyond Earth's mass**; the day we do, that pipeline uses Pozzo. Recorded, not decided. (RM22 rejects
Pozzo's 100 W/m/K for k and fits 60.)

## 3. Pre-registered outcomes

- ① The branch exists in `dynamo_rocky`: a declared free rotator is dipolar by rule (no Ro_ℓ, base moment); a
  declared locked body enters the locked branch and its Ro_ℓ is refused with the three names on the note.
- ② Roster: Earth (free, undeclared lock → the branch says which) and Pandora (locked on the board, declared
  nowhere the chain reads → the branch reports the missing declaration by name). **No expectation on values.**
- ③ The `MULTIPOLAR_FACTORS` consumer count on the roster: **reported, no expectation** — zero → C5 question
  to the owner, non-zero → which bodies.
- ④ The edge note corrected; the `tidal_locking`-has-no-recipe fact recorded in `chain.yaml` beside the node.
- ⑤ Anchors byte-identical; `dynamo_rocky` is not a path-fingerprint function; no `--refresh`.
- ⑥ Gate FAIL 0, time, `pmset` both ends. ⑧ Any new declaration/output → Needs line + doc row (en + ko) in the
  same commit.
