# Checklist: partial differentiation — closing C7 as "not a mixture"

Plan in one line: keep the refusal for `differentiated: false` with ice or gas, raise its
reason from "the rule handles rock and metal only" to "water in rock is a reaction and a
transport history, modelled as thermal evolution, not a composition", state the source
limit plainly, and fence C10 off from the finding.

- [x] three Icarus bibcodes and DOIs checked against ADS by title; all PUB_HTML only, abstracts read, full texts not
- [x] refusal string in `solve` raised (a path function: `--refresh`, values bit-identical)
- [x] domain row `differentiated: false` + Korean mirror
- [x] C7 row closed, dated, with the two reasons, the provenance limit, the request-list line (C7 + C9), and the C10 fence
- [x] citations (EN + KO) for the three papers, marked abstract-only
- [x] no anchor moves; gate FAIL 0; report to `nearstars-cb`
